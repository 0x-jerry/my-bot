FROM oven/bun:1-debian

RUN apt-get update
RUN apt-get install curl -y

RUN curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh

RUN mv ~/.local/bin/rtk /usr/bin/rtk

RUN apt-get install git -y

COPY data /root/

RUN rtk init -g
